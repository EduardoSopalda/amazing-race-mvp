{
  "name": "barcelona-race",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GPS-based team-building race engine for Barcelona.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
